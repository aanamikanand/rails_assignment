<h1> Lists </h1>
<br/>


<% if @lists.any? %>

<ul>
	<% @lists.each do |list| %>
	<li> 
		<%= link_to list.name, list_path(list) %>
	</li>
	<% end %>
</ul>
<% else %>
	<h2> No lists found. Please create one! </h2>
<% end %>

<%= link_to 'New List', new_list_path %>